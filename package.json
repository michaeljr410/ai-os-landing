{
  "name": "ai-os-landing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "resend": "^4.1.0",
    "stripe": "^17.5.0"
  }
}
